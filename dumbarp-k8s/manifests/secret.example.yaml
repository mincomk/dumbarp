apiVersion: v1
kind: Secret
metadata:
  name: dumbarp-k8s-tokens
  namespace: kube-system
type: Opaque
stringData:
  # File names here must match the `auth_token_file` paths in the ConfigMap.
  homelab: "REPLACE_WITH_DAEMON_AUTH_TOKEN"
  edge:    "REPLACE_WITH_DAEMON_AUTH_TOKEN"
